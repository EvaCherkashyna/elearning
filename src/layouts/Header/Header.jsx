import React from 'react'
import { HeaderBanner, HeaderNav } from '../../components'

const Header = () => {
  return (
    <header className=''>
      <HeaderBanner />
      <HeaderNav />
    </header>
  )
}

export default Header
